# Name of your application. Used to uniquely configure containers.
service: demofrog

# Name of the container image.
image: adammusa/demofrog

# Deploy to these servers.
servers:
  web:
    hosts:
              - 5.78.46.167


# Branded error pages served by kamal-proxy (503 shows the maintenance message).
error_pages_path: .alkimist/error_pages

# SSH from the user's account container. Deploys must be non-interactive.
ssh:
  user: root
  keys_only: true
  keys:
    - /home/deployer/.ssh/id_rsa
  config: /home/deployer/.ssh/config

proxy:
  ssl: true
  hosts:
    - dartfrog.alkimist.dev
  app_port: 8080
  healthcheck:
    path: /up


# Credentials for your image host.
registry:
  server: localhost:10035

env:
  clear:
    PORT: 8080


# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
aliases:
  shell: app exec --interactive --reuse "sh"
  logs: app logs


# Build on the project's own VPS: Alkimist orchestrates, your server builds.
# Alkimist provisions a project-scoped transfer registry and SSH-forwards
# its unique port to the VPS, allowing independent concurrent builds.
builder:
  arch: amd64
  local: false
  remote: ssh://root@5.78.46.167
